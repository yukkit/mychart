apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linkoopdb.fullname" . }}-test-connection"
  labels:
{{ include "linkoopdb.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "linkoopdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
